# Preenchidos automaticamente por ./deploy-contratos.sh a cada implantacao.
ENDERECO_CONTROLE_ACESSO=
ENDERECO_REGISTRO_DOCUMENTOS=

# No local, e a primeira conta do anvil (chave publica e conhecida, sem risco).
CHAVE_PRIVADA_ADMIN=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

BLOCKCHAIN_RPC_URL=http://127.0.0.1:8545

# Gere o seu:  python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET_KEY=

# Fernet, cifra os dados pessoais em repouso.
# Gere com:  python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
#
# ATENCAO: trocar esta chave torna ilegivel tudo que ja foi gravado no banco.
# Se for continuar usando um banco.db existente, mantenha a chave que o gerou.
CHAVE_CRIPTOGRAFIA_DADOS_SENSIVEIS=
